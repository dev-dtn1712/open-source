import { StyleSheet } from 'react-native';
import { APP_DARK_GRAY } from '../../Themes/Colors';

export default StyleSheet.create({
  applicationView: {
    backgroundColor: APP_DARK_GRAY,
    flex: 1
  }
});
